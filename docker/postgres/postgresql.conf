# Минимальные настройки для dev/staging в Docker.
# Полный список параметров: https://www.postgresql.org/docs/current/runtime-config.html

listen_addresses = '*'
max_connections = 100
shared_buffers = 128MB
log_timezone = 'UTC'
timezone = 'UTC'
